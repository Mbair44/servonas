import AuthForm from "@/components/AuthForm"; import {requestPasswordReset} from "../auth/actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams; if(q.sent) return <main className="auth-shell"><section className="auth-card"><h1>Check your email</h1><p>We sent a secure password-reset link. You can close this page after opening it.</p></section></main>; return <AuthForm title="Reset your password" subtitle="Enter your email and we'll send a secure reset link." action={requestPasswordReset} mode="forgot" error={q.error}/>}
