import {NextResponse} from "next/server";
import {requireWorkspace} from "@/lib/workspace";
import {processAssistantInput} from "@/lib/assistant/orchestrator";

export async function GET(_request:Request,{params}:{params:Promise<{businessSlug:string}>}){const {businessSlug}=await params,{supabase,business,user}=await requireWorkspace(businessSlug);const {data:conversation,error}=await supabase.from("ai_conversations").select("id").eq("business_id",business.id).eq("user_id",user.id).eq("channel","web").order("updated_at",{ascending:false}).limit(1).maybeSingle();if(error)return NextResponse.json({error:"Assistant conversation could not be loaded."},{status:500});if(!conversation)return NextResponse.json({conversationId:null,messages:[]});const {data:messages,error:messageError}=await supabase.from("ai_messages").select("id,role,content,metadata").eq("conversation_id",conversation.id).eq("business_id",business.id).eq("user_id",user.id).in("role",["user","assistant"]).order("created_at").limit(100);if(messageError)return NextResponse.json({error:"Assistant messages could not be loaded."},{status:500});return NextResponse.json({conversationId:conversation.id,messages:(messages??[]).map((message:any)=>({id:message.id,role:message.role,content:message.content,actionRequest:message.metadata?.actionRequest}))});}

export async function POST(request:Request,{params}:{params:Promise<{businessSlug:string}>}){const {businessSlug}=await params,{supabase,business,user,role}=await requireWorkspace(businessSlug);let body:{input?:string;conversationId?:string|null;channel?:string};try{body=await request.json();}catch{return NextResponse.json({error:"Enter a message."},{status:400});}if(body.channel&&body.channel!=="web")return NextResponse.json({error:"This interface supports web conversations only."},{status:400});try{return NextResponse.json(await processAssistantInput({context:{supabase,business,user,role,conversationId:"",channel:"web"},conversationId:body.conversationId,channel:"web",input:String(body.input??"")}));}catch(error){console.error("Assistant request failed",{businessId:business.id,userId:user.id,errorName:error instanceof Error?error.name:"unknown"});return NextResponse.json({error:error instanceof Error?error.message:"I couldn't complete that request."},{status:400});}}
