import {NextResponse} from "next/server";import Stripe from "stripe";import {resolveBookingManageToken} from "@/lib/bookingManage/tokens";import {getSupabaseAdmin} from "@/lib/supabaseAdmin";
export async function POST(_:Request,{params}:{params:Promise<{token:string}>}){const {token}=await params,access=await resolveBookingManageToken(token),db=getSupabaseAdmin(),key=process.env.STRIPE_SECRET_KEY;if(!access||!db||!key)return NextResponse.json({error:"Link unavailable"},{status:404});const {data:b}=await db.from("bookings").select("id,booking_number,balance_due_cents,status,business_payment_accounts(provider_account_id,charges_enabled)").eq("id",access.booking_id).eq("business_id",access.business_id).maybeSingle();if(!b)return NextResponse.json({error:"This booking cannot accept payments."},{status:404});if(Number(b.balance_due_cents)<=0)return NextResponse.json({error:"No balance is due."},{status:409});if(!["pending_payment","paid","confirmed","completed"].includes(b.status))return NextResponse.json({error:"This booking cannot accept payments."},{status:409});const account=Array.isArray(b.business_payment_accounts)?b.business_payment_accounts[0]:b.business_payment_accounts;if(!account?.provider_account_id||!account.charges_enabled)return NextResponse.json({error:"Payments are unavailable."},{status:503});const session=await new Stripe(key).checkout.sessions.create({mode:"payment",line_items:[{quantity:1,price_data:{currency:"usd",unit_amount:Number(b.balance_due_cents),product_data:{name:`Remaining balance · Booking #${b.booking_number}`}}}],success_url:`${process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000"}/manage-booking/${token}`,cancel_url:`${process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000"}/manage-booking/${token}`,metadata:{booking_id:b.id,business_id:access.business_id,payment_kind:"customer_balance"},payment_intent_data:{setup_future_usage:"off_session"}},{stripeAccount:account.provider_account_id});await db.from("booking_change_audit").insert({booking_id:b.id,business_id:access.business_id,change_source:"customer",change_type:"balance_payment_started",new_values:{amount_cents:b.balance_due_cents},payment_reference:session.id,resulting_balance_due_cents:b.balance_due_cents});return NextResponse.redirect(session.url!,303)}
