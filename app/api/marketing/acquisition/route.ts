import {NextResponse} from "next/server";
import {acquisitionEvents,attributionKeys,normalizeAcquisitionLandingPagePath,safeAcquisitionMetadata,validSessionId,type AttributionValues} from "@/lib/acquisitionFunnel";
import {getSupabaseAdmin} from "@/lib/supabaseAdmin";
const bots=/bot|crawler|spider|facebookexternalhit|googleother|headless|lighthouse|playwright|puppeteer/i;
const clean=(value:unknown,max=1000)=>typeof value==="string"?value.trim().slice(0,max):"";
const device=(ua:string)=>/ipad|tablet/i.test(ua)?"tablet":/mobile|android|iphone/i.test(ua)?"mobile":"desktop";
export async function POST(request:Request){const purpose=request.headers.get("purpose")||request.headers.get("x-middleware-prefetch")||"",ua=request.headers.get("user-agent")||"";if(/prefetch/i.test(purpose)||bots.test(ua))return new NextResponse(null,{status:204});const body=await request.json().catch(()=>null) as {sessionId?:string;industry?:string;event?:string;path?:string;landingUrl?:string;referrer?:string;attribution?:AttributionValues;metadata?:object}|null;if(!body||!validSessionId(body.sessionId)||!body.industry||!acquisitionEvents.has(body.event as never))return new NextResponse(null,{status:400});const db=getSupabaseAdmin();if(!db)return new NextResponse(null,{status:204});const now=new Date().toISOString(),attribution=body.attribution??{},row:Record<string,unknown>={id:body.sessionId,industry:clean(body.industry,80),first_landing_path:normalizeAcquisitionLandingPagePath(clean(body.path),clean(body.landingUrl,2000)),first_landing_url:clean(body.landingUrl,2000),first_referrer:clean(body.referrer,2000),device_category:device(ua),last_seen_at:now,updated_at:now};for(const key of attributionKeys)row[key]=clean(attribution[key],500)||null;const inserted=await db.from("website_acquisition_sessions").insert(row);if(inserted.error&&inserted.error.code!=="23505")return new NextResponse(null,{status:204});if(inserted.error)await db.from("website_acquisition_sessions").update({last_seen_at:now,updated_at:now}).eq("id",body.sessionId).eq("industry",row.industry);const event=body.event as string,key=["marketing_landing_view","website_builder_started","website_builder_step1_started","website_builder_style_viewed","website_preview_viewed"].includes(event)?`${body.sessionId}:${event}`:null;await db.from("website_acquisition_events").insert({acquisition_session_id:body.sessionId,industry:row.industry,event_name:event,event_key:key,metadata:safeAcquisitionMetadata(body.metadata)});return new NextResponse(null,{status:204});}
