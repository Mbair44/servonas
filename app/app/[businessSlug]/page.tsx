import Link from "next/link";
export default async function Workspace({params}:{params:Promise<{businessSlug:string}>}){const {businessSlug}=await params; const name=businessSlug.split("-").map(x=>x[0]?.toUpperCase()+x.slice(1)).join(" ");return <main className="sv-workspace"><aside><img src="/servonas-logo-light.svg" alt="Servonas"/><nav><b>Overview</b><span>Bookings</span><span>Calendar</span><span>Inventory</span><span>Customers</span><span>Payments</span><span>Settings</span></nav></aside><section><div className="sv-workspace-head"><div><small>Workspace preview</small><h1>{name}</h1></div><Link href="/onboarding">Edit setup</Link></div><div className="sv-work-metrics"><article><small>Revenue</small><strong>$0</strong><span>Connect Stripe to begin</span></article><article><small>Upcoming bookings</small><strong>0</strong><span>Your calendar is clear</span></article><article><small>Customers</small><strong>0</strong><span>Ready for your first lead</span></article></div><div className="sv-empty"><h2>Your workspace foundation is ready.</h2><p>The next implementation milestone connects Supabase Auth, creates the tenant record from onboarding, and scopes every booking, item, customer, and team member to this business.</p><Link className="sv-button" href="/demo">View Product Demo</Link></div></section></main>}
