import {notFound} from "next/navigation";import {getSupabaseAdmin} from "@/lib/supabaseAdmin";import {PromotionLanding} from "@/components/PromotionLanding";
export const dynamic="force-dynamic";
export default async function PromotionPage({params}:{params:Promise<{siteSlug:string;promotionSlug:string}>}){const {siteSlug,promotionSlug}=await params,db=getSupabaseAdmin();if(!db)notFound();const {data:website}=await db.from("business_website_settings").select("business_id").ilike("public_slug",siteSlug).eq("status","published").maybeSingle();if(!website)notFound();const {data:promotion}=await db.from("promotions").select("*,discounts(*)").eq("business_id",website.business_id).ilike("slug",promotionSlug).eq("landing_page_enabled",true).maybeSingle();if(!promotion)notFound();const [{data:business},{data:items}]=await Promise.all([db.from("businesses").select("name,primary_color").eq("id",website.business_id).single(),db.from("inventory_items").select("id,name,daily_price_cents,image_url,category_id").eq("business_id",website.business_id).eq("active",true)]);const categoryIds=new Set((await db.from("promotion_categories").select("category_id").eq("promotion_id",promotion.id)).data?.map(row=>row.category_id)??[]);const selected=categoryIds.size?(items??[]).filter(item=>categoryIds.has(item.category_id)):items??[];return <PromotionLanding promotion={promotion} business={business} items={selected} baseBookingUrl={`/book/${siteSlug}`}/>;}
