import Link from "next/link";
const money=(cents:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(cents/100);
export function CategoryLanding({page,business,items,bookingUrl}:{page:any;business:any;items:any[];bookingUrl:string}){return <main className="promotion-landing" style={{"--promotion-brand":business.primaryColor??business.primary_color??"#1769f5"} as React.CSSProperties}><header className="promotion-landing-header"><Link href={bookingUrl} className="promotion-brand">{business.logoUrl?<img src={business.logoUrl} alt={`${business.name} logo`}/>:<i>{business.name.slice(0,1)}</i>}<strong>{business.name}</strong></Link><a className="promotion-header-cta" href="#available">Check Availability</a></header><section className="promotion-hero"><div><small>{business.name}</small><h1>{page.title}</h1><p>{page.intro||`Browse available ${page.title.toLowerCase()} and check your event date online.`}</p></div></section><section className="promotion-rentals" id="available"><header><div><small>Available rentals</small><h2>{page.title}</h2></div></header><div className="promotion-rental-grid">{items.map(item=><article className="promotion-rental-card" key={item.id}><div className="promotion-rental-image">{item.image_url?<img src={item.image_url} alt={item.name}/>:<span>{item.name.slice(0,1)}</span>}</div><div><h3>{item.name}</h3><p className="promotion-price"><strong>{money(item.daily_price_cents)}</strong><span> per day</span></p><Link href={`${bookingUrl}?item=${item.id}`}>Check Availability</Link></div></article>)}</div></section></main>}
