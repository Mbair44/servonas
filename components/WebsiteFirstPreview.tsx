import Link from "next/link";
import {finishWebsiteFirstOnboarding} from "@/app/onboarding/actions";
import {getWebsiteFirstConfig,type WebsiteFirstSource} from "@/lib/websiteFirstConfig";
export function WebsiteFirstPreview({businessSlug,source}:{businessSlug:string;source:WebsiteFirstSource}){const config=getWebsiteFirstConfig(source)!;return <div className="website-first-preview"><header><span className="complete">✓ Business</span><i/><span className="complete">✓ Style</span><i/><span>3. Preview</span></header><section><span className="sv-kicker">Your Website Is Ready</span><h1>Take a look around.</h1><p>You can customize anything before publishing.</p><div className="website-first-preview-frame"><iframe src={`/app/${businessSlug}/settings/website/preview`} title={`Your ${config.industryLabel} website preview`}/></div><div className="website-first-preview-actions"><Link className="sv-button" href={`/app/${businessSlug}/settings/website`}>Customize My Website</Link><form action={finishWebsiteFirstOnboarding.bind(null,businessSlug)}><button className="sv-button sv-secondary">Continue Setting Up My Business</button></form></div><aside><strong>When you&apos;re ready, continue with:</strong>{config.progressiveActions.map(action=><span key={action}>{action}</span>)}</aside></section></div>}
