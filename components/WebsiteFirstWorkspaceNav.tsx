import Link from "next/link";
export function WebsiteFirstWorkspaceNav({slug,name}:{slug:string;name:string}){return <aside className="epic3-sidebar website-first-sidebar"><div className="workspace-nav-header"><Link href={`/app/${slug}/settings/website`} className="epic3-brand"><img src="/servonas-logo-light.svg" alt="Servonas"/></Link></div><small className="workspace-context">{name}</small><div className="website-first-nav-message"><span>Website improvements</span><strong>Your site is ready to publish.</strong><p>Launch first, then come back anytime to improve content, domain, reviews, booking, or design.</p></div><nav aria-label="Focused website setup navigation"><Link className="active" href={`/app/${slug}/settings/website`}>Improve website</Link><Link href={`/app/${slug}/settings/website/preview`} target="_blank">Preview website</Link></nav><div className="website-first-nav-note"><b>Your progress is saved</b><span>You can safely return later.</span></div></aside>}
