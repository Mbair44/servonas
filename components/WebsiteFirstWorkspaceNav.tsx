import Link from "next/link";
export function WebsiteFirstWorkspaceNav({slug,name}:{slug:string;name:string}){return <aside className="epic3-sidebar website-first-sidebar"><div className="workspace-nav-header"><Link href={`/app/${slug}/settings/website`} className="epic3-brand"><img src="/servonas-logo-light.svg" alt="Servonas"/></Link></div><small className="workspace-context">{name}</small><div className="website-first-nav-message"><span>Website setup</span><strong>Let&apos;s get your website online.</strong><p>Complete the seven website steps, review your live preview, and publish when you&apos;re ready.</p></div><nav aria-label="Focused website setup navigation"><Link className="active" href={`/app/${slug}/settings/website`}>Website setup</Link><Link href={`/app/${slug}/settings/website/preview`} target="_blank">Preview website</Link></nav><div className="website-first-nav-note"><b>Your progress is saved</b><span>You can safely return later.</span></div></aside>}
