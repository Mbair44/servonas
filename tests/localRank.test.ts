import test from "node:test";import assert from "node:assert/strict";import {localRankSuggestions,rankBand} from "../lib/localRank.ts";
test("rank labels always retain numeric rank semantics",()=>{assert.equal(rankBand(3),"strong");assert.equal(rankBand(10),"moderate");assert.equal(rankBand(20),"weak");assert.equal(rankBand(null),"poor");});test("party rental suggestions include Copper State Bounce searches",()=>{const values=localRankSuggestions({industry:"party_rental",services:[],areas:[]});assert.ok(values.includes("bounce house rental"));assert.ok(values.includes("mechanical bull rental"));});
