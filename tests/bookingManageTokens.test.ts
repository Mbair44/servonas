import test from "node:test";import assert from "node:assert/strict";import {readFileSync} from "node:fs";test("manage portal uses a hashed revocable token",()=>{const sql=readFileSync(new URL("../supabase/migrations/20260918000300_booking_manage_portal.sql",import.meta.url),"utf8"),lib=readFileSync(new URL("../lib/bookingManage/tokens.ts",import.meta.url),"utf8");assert.match(sql,/token_hash/);assert.match(sql,/revoked_at/);assert.match(sql,/booking_change_audit/);assert.match(lib,/randomBytes\(32\)/);assert.match(lib,/sha256/);assert.match(lib,/revoked_at:new Date/);});
