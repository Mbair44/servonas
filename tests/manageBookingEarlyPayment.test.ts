import test from "node:test";import assert from "node:assert/strict";import {readFileSync} from "node:fs";test("manage booking uses active rental status and authoritative balance",()=>{const source=readFileSync(new URL("../app/api/manage-booking/[token]/pay/route.ts",import.meta.url),"utf8");assert.match(source,/\["pending_payment","paid","confirmed","completed"\]/);assert.match(source,/No balance is due/);assert.match(source,/This booking cannot accept payments/);assert.doesNotMatch(source,/balance_charge_scheduled_for/);});
